namespace Microsoft.Maui.Controls
{
    public static class Constants
    {
        public static RelativeBindingSource AncestorBindingContext = new RelativeBindingSource(RelativeBindingSourceMode.FindAncestorBindingContext, typeof(object));
    }
}
